<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aidotnet.erp.fms.infrastructure.mapper.FmsExtMapper">

    <insert id="insertCostBreakdown" parameterType="com.aidotnet.erp.fms.infrastructure.data.CostBreakdownDO">
        INSERT INTO fms_cost_breakdown (breakdown_id, tenant_id, cost_event_id, cost_type, cost_category, amount, currency, exchange_rate, amount_in_base_currency, remark, created_at)
        VALUES (#{breakdownId}, #{tenantId}, #{costEventId}, #{costType}, #{costCategory}, #{amount}, #{currency}, #{exchangeRate}, #{amountInBaseCurrency}, #{remark}, #{createdAt})
    </insert>

    <select id="selectCostBreakdownsByEvent" resultType="com.aidotnet.erp.fms.infrastructure.data.CostBreakdownDO">
        SELECT breakdown_id, tenant_id, cost_event_id, cost_type, cost_category, amount, currency, exchange_rate, amount_in_base_currency, remark, created_at
        FROM fms_cost_breakdown WHERE tenant_id = #{tenantId} AND cost_event_id = #{costEventId}
        ORDER BY created_at ASC
    </select>

    <select id="selectCostBreakdownsByType" resultType="com.aidotnet.erp.fms.infrastructure.data.CostBreakdownDO">
        SELECT breakdown_id, tenant_id, cost_event_id, cost_type, cost_category, amount, currency, exchange_rate, amount_in_base_currency, remark, created_at
        FROM fms_cost_breakdown WHERE tenant_id = #{tenantId} AND cost_type = #{costType}
        ORDER BY created_at DESC
    </select>

    <insert id="insertJournalEntry" parameterType="com.aidotnet.erp.fms.infrastructure.data.JournalEntryDO">
        INSERT INTO fms_journal_entry (entry_id, tenant_id, account_code, account_name, type, amount, currency, reference_type, reference_id, remark, entry_date, created_at)
        VALUES (#{entryId}, #{tenantId}, #{accountCode}, #{accountName}, #{type}, #{amount}, #{currency}, #{referenceType}, #{referenceId}, #{remark}, #{entryDate}, #{createdAt})
    </insert>

    <select id="selectJournalEntriesByReference" resultType="com.aidotnet.erp.fms.infrastructure.data.JournalEntryDO">
        SELECT entry_id, tenant_id, account_code, account_name, type, amount, currency, reference_type, reference_id, remark, entry_date, created_at
        FROM fms_journal_entry WHERE tenant_id = #{tenantId} AND reference_type = #{referenceType} AND reference_id = #{referenceId}
        ORDER BY entry_date ASC
    </select>

    <select id="selectJournalEntriesByAccount" resultType="com.aidotnet.erp.fms.infrastructure.data.JournalEntryDO">
        SELECT entry_id, tenant_id, account_code, account_name, type, amount, currency, reference_type, reference_id, remark, entry_date, created_at
        FROM fms_journal_entry WHERE tenant_id = #{tenantId} AND account_code = #{accountCode}
        ORDER BY entry_date DESC
    </select>

    <insert id="insertTaxRule" parameterType="com.aidotnet.erp.fms.infrastructure.data.TaxRuleDO">
        INSERT INTO fms_tax_rule (rule_id, tenant_id, country_code, tax_type, tax_rate, tax_category, enabled, effective_from, effective_to, created_at, updated_at)
        VALUES (#{ruleId}, #{tenantId}, #{countryCode}, #{taxType}, #{taxRate}, #{taxCategory}, #{enabled}, #{effectiveFrom}, #{effectiveTo}, #{createdAt}, #{updatedAt})
    </insert>

    <update id="updateTaxRule" parameterType="com.aidotnet.erp.fms.infrastructure.data.TaxRuleDO">
        UPDATE fms_tax_rule SET tax_rate = #{taxRate}, tax_category = #{taxCategory}, enabled = #{enabled},
               effective_from = #{effectiveFrom}, effective_to = #{effectiveTo}, updated_at = #{updatedAt}
        WHERE rule_id = #{ruleId} AND tenant_id = #{tenantId}
    </update>

    <select id="selectTaxRule" resultType="com.aidotnet.erp.fms.infrastructure.data.TaxRuleDO">
        SELECT rule_id, tenant_id, country_code, tax_type, tax_rate, tax_category, enabled, effective_from, effective_to, created_at, updated_at
        FROM fms_tax_rule WHERE tenant_id = #{tenantId} AND rule_id = #{ruleId}
    </select>

    <select id="selectTaxRules" resultType="com.aidotnet.erp.fms.infrastructure.data.TaxRuleDO">
        SELECT rule_id, tenant_id, country_code, tax_type, tax_rate, tax_category, enabled, effective_from, effective_to, created_at, updated_at
        FROM fms_tax_rule WHERE tenant_id = #{tenantId} AND country_code = #{countryCode}
        ORDER BY created_at DESC
    </select>

    <select id="selectActiveTaxRule" resultType="com.aidotnet.erp.fms.infrastructure.data.TaxRuleDO">
        SELECT rule_id, tenant_id, country_code, tax_type, tax_rate, tax_category, enabled, effective_from, effective_to, created_at, updated_at
        FROM fms_tax_rule WHERE tenant_id = #{tenantId} AND country_code = #{countryCode} AND tax_type = #{taxType}
              AND enabled = true AND effective_from &lt;= CURRENT_TIMESTAMP
              AND (effective_to IS NULL OR effective_to &gt; CURRENT_TIMESTAMP)
        LIMIT 1
    </select>

    <insert id="insertInvoice" parameterType="com.aidotnet.erp.fms.infrastructure.data.InvoiceDO">
        INSERT INTO fms_invoice (
            invoice_id, tenant_id, invoice_no, invoice_type, customer_id,
            customer_name, country_code, currency, subtotal_amount, tax_amount,
            total_amount, tax_id_number, status, voucher_id, invoice_date,
            due_date, remark, created_at, updated_at
        )
        VALUES (
            #{invoiceId}, #{tenantId}, #{invoiceNumber}, #{invoiceType}, #{customerId},
            #{customerName}, #{countryCode}, #{currency}, #{subtotalAmount}, #{taxAmount},
            #{totalAmount}, #{taxIdNumber}, #{status}, #{voucherId}, #{invoiceDate},
            #{dueDate}, #{remark}, #{createdAt}, #{updatedAt}
        )
    </insert>

    <update id="updateInvoice" parameterType="com.aidotnet.erp.fms.infrastructure.data.InvoiceDO">
        UPDATE fms_invoice
        SET invoice_no = #{invoiceNumber},
            invoice_type = #{invoiceType},
            customer_id = #{customerId},
            customer_name = #{customerName},
            country_code = #{countryCode},
            currency = #{currency},
            subtotal_amount = #{subtotalAmount},
            tax_amount = #{taxAmount},
            total_amount = #{totalAmount},
            tax_id_number = #{taxIdNumber},
            status = #{status},
            voucher_id = #{voucherId},
            invoice_date = #{invoiceDate},
            due_date = #{dueDate},
            remark = #{remark},
            updated_at = #{updatedAt}
        WHERE tenant_id = #{tenantId}
          AND invoice_id = #{invoiceId}
    </update>

    <select id="selectInvoice" resultType="com.aidotnet.erp.fms.infrastructure.data.InvoiceDO">
        SELECT invoice_id,
               tenant_id,
               invoice_no AS invoice_number,
               invoice_type,
               customer_id,
               customer_name,
               country_code,
               currency,
               subtotal_amount,
               tax_amount,
               total_amount,
               tax_id_number,
               status,
               voucher_id,
               invoice_date,
               due_date,
               remark,
               created_at,
               updated_at
        FROM fms_invoice
        WHERE tenant_id = #{tenantId}
          AND invoice_id = #{invoiceId}
    </select>

    <select id="selectInvoices" resultType="com.aidotnet.erp.fms.infrastructure.data.InvoiceDO">
        SELECT invoice_id,
               tenant_id,
               invoice_no AS invoice_number,
               invoice_type,
               customer_id,
               customer_name,
               country_code,
               currency,
               subtotal_amount,
               tax_amount,
               total_amount,
               tax_id_number,
               status,
               voucher_id,
               invoice_date,
               due_date,
               remark,
               created_at,
               updated_at
        FROM fms_invoice
        WHERE tenant_id = #{tenantId}
        <if test="countryCode != null and countryCode != ''">
          AND country_code = #{countryCode}
        </if>
        <if test="status != null and status != ''">
          AND status = #{status}
        </if>
        ORDER BY created_at DESC, invoice_id DESC
    </select>

    <insert id="insertVoucherTemplate" parameterType="com.aidotnet.erp.fms.infrastructure.data.VoucherTemplateDO">
        INSERT INTO fms_voucher_template (
            template_id, tenant_id, template_name, business_type, debit_account,
            credit_account, description, enabled, created_at, updated_at
        )
        VALUES (
            #{templateId}, #{tenantId}, #{templateName}, #{businessType}, #{debitAccount},
            #{creditAccount}, #{description}, #{enabled}, #{createdAt}, #{updatedAt}
        )
    </insert>

    <update id="updateVoucherTemplate" parameterType="com.aidotnet.erp.fms.infrastructure.data.VoucherTemplateDO">
        UPDATE fms_voucher_template
        SET template_name = #{templateName},
            business_type = #{businessType},
            debit_account = #{debitAccount},
            credit_account = #{creditAccount},
            description = #{description},
            enabled = #{enabled},
            updated_at = #{updatedAt}
        WHERE tenant_id = #{tenantId}
          AND template_id = #{templateId}
    </update>

    <select id="selectVoucherTemplate" resultType="com.aidotnet.erp.fms.infrastructure.data.VoucherTemplateDO">
        SELECT template_id, tenant_id, template_name, business_type, debit_account,
               credit_account, description, enabled, created_at, updated_at
        FROM fms_voucher_template
        WHERE tenant_id = #{tenantId}
          AND template_id = #{templateId}
    </select>

    <select id="selectVoucherTemplates" resultType="com.aidotnet.erp.fms.infrastructure.data.VoucherTemplateDO">
        SELECT template_id, tenant_id, template_name, business_type, debit_account,
               credit_account, description, enabled, created_at, updated_at
        FROM fms_voucher_template
        WHERE tenant_id = #{tenantId}
        <if test="businessType != null and businessType != ''">
          AND business_type = #{businessType}
        </if>
        ORDER BY created_at DESC, template_id DESC
    </select>

    <insert id="insertVoucher" parameterType="com.aidotnet.erp.fms.infrastructure.data.VoucherDO">
        INSERT INTO fms_voucher (
            voucher_id, tenant_id, voucher_no, voucher_type, reference_type,
            reference_id, currency, debit_total, credit_total, status,
            lines_json, voucher_date, posted_by, posted_at, export_batch_id,
            created_at, updated_at
        )
        VALUES (
            #{voucherId}, #{tenantId}, #{voucherNumber}, #{voucherType}, #{referenceType},
            #{referenceId}, #{currency}, #{totalDebit}, #{totalCredit}, #{status},
            CAST(#{linesJson} AS JSONB), #{voucherDate}, #{postedBy}, #{postedAt}, #{exportBatchId},
            #{createdAt}, #{updatedAt}
        )
    </insert>

    <update id="updateVoucher" parameterType="com.aidotnet.erp.fms.infrastructure.data.VoucherDO">
        UPDATE fms_voucher
        SET voucher_no = #{voucherNumber},
            voucher_type = #{voucherType},
            reference_type = #{referenceType},
            reference_id = #{referenceId},
            currency = #{currency},
            debit_total = #{totalDebit},
            credit_total = #{totalCredit},
            status = #{status},
            lines_json = CAST(#{linesJson} AS JSONB),
            voucher_date = #{voucherDate},
            posted_by = #{postedBy},
            posted_at = #{postedAt},
            export_batch_id = #{exportBatchId},
            updated_at = #{updatedAt}
        WHERE tenant_id = #{tenantId}
          AND voucher_id = #{voucherId}
    </update>

    <select id="selectVoucher" resultType="com.aidotnet.erp.fms.infrastructure.data.VoucherDO">
        SELECT voucher_id, tenant_id, voucher_no AS voucher_number, voucher_type, reference_type,
               reference_id, currency, debit_total AS total_debit, credit_total AS total_credit, status,
               CAST(lines_json AS TEXT) AS lines_json, voucher_date, posted_by, posted_at,
               export_batch_id, created_at, updated_at
        FROM fms_voucher
        WHERE tenant_id = #{tenantId}
          AND voucher_id = #{voucherId}
    </select>

    <select id="selectVouchers" resultType="com.aidotnet.erp.fms.infrastructure.data.VoucherDO">
        SELECT voucher_id, tenant_id, voucher_no AS voucher_number, voucher_type, reference_type,
               reference_id, currency, debit_total AS total_debit, credit_total AS total_credit, status,
               CAST(lines_json AS TEXT) AS lines_json, voucher_date, posted_by, posted_at,
               export_batch_id, created_at, updated_at
        FROM fms_voucher
        WHERE tenant_id = #{tenantId}
        <if test="voucherType != null and voucherType != ''">
          AND voucher_type = #{voucherType}
        </if>
        <if test="status != null and status != ''">
          AND status = #{status}
        </if>
        ORDER BY created_at DESC, voucher_id DESC
    </select>

    <select id="selectVouchersByReference" resultType="com.aidotnet.erp.fms.infrastructure.data.VoucherDO">
        SELECT voucher_id, tenant_id, voucher_no AS voucher_number, voucher_type, reference_type,
               reference_id, currency, debit_total AS total_debit, credit_total AS total_credit, status,
               CAST(lines_json AS TEXT) AS lines_json, voucher_date, posted_by, posted_at,
               export_batch_id, created_at, updated_at
        FROM fms_voucher
        WHERE tenant_id = #{tenantId}
          AND reference_type = #{referenceType}
          AND reference_id = #{referenceId}
        ORDER BY created_at DESC, voucher_id DESC
    </select>

    <insert id="insertInvoiceSetting" parameterType="com.aidotnet.erp.fms.infrastructure.data.InvoiceSettingDO">
        INSERT INTO fms_invoice_setting (
            setting_id, tenant_id, store_id, marketplace_id, template_id, invoice_title,
            tax_registration_no, show_unit_price, show_tax_rate, show_discount, remark,
            enabled, created_at, updated_at
        )
        VALUES (
            #{settingId}, #{tenantId}, #{storeId}, #{marketplaceId}, #{templateId}, #{invoiceTitle},
            #{taxRegistrationNo}, #{showUnitPrice}, #{showTaxRate}, #{showDiscount}, #{remark},
            #{enabled}, #{createdAt}, #{updatedAt}
        )
    </insert>

    <update id="updateInvoiceSetting" parameterType="com.aidotnet.erp.fms.infrastructure.data.InvoiceSettingDO">
        UPDATE fms_invoice_setting
        SET store_id = #{storeId},
            marketplace_id = #{marketplaceId},
            template_id = #{templateId},
            invoice_title = #{invoiceTitle},
            tax_registration_no = #{taxRegistrationNo},
            show_unit_price = #{showUnitPrice},
            show_tax_rate = #{showTaxRate},
            show_discount = #{showDiscount},
            remark = #{remark},
            enabled = #{enabled},
            updated_at = #{updatedAt}
        WHERE tenant_id = #{tenantId}
          AND setting_id = #{settingId}
    </update>

    <select id="selectInvoiceSetting" resultType="com.aidotnet.erp.fms.infrastructure.data.InvoiceSettingDO">
        SELECT setting_id, tenant_id, store_id, marketplace_id, template_id, invoice_title,
               tax_registration_no, show_unit_price, show_tax_rate, show_discount, remark,
               enabled, created_at, updated_at
        FROM fms_invoice_setting
        WHERE tenant_id = #{tenantId}
          AND setting_id = #{settingId}
    </select>

    <select id="selectInvoiceSettingByScope" resultType="com.aidotnet.erp.fms.infrastructure.data.InvoiceSettingDO">
        SELECT setting_id, tenant_id, store_id, marketplace_id, template_id, invoice_title,
               tax_registration_no, show_unit_price, show_tax_rate, show_discount, remark,
               enabled, created_at, updated_at
        FROM fms_invoice_setting
        WHERE tenant_id = #{tenantId}
          AND store_id = #{storeId}
          AND (
                (#{marketplaceId} IS NULL AND marketplace_id IS NULL)
                OR marketplace_id = #{marketplaceId}
              )
        ORDER BY updated_at DESC, setting_id DESC
        LIMIT 1
    </select>

    <select id="selectInvoiceSettings" resultType="com.aidotnet.erp.fms.infrastructure.data.InvoiceSettingDO">
        SELECT setting_id, tenant_id, store_id, marketplace_id, template_id, invoice_title,
               tax_registration_no, show_unit_price, show_tax_rate, show_discount, remark,
               enabled, created_at, updated_at
        FROM fms_invoice_setting
        WHERE tenant_id = #{tenantId}
        ORDER BY updated_at DESC, setting_id DESC
    </select>

    <insert id="insertExternalFinanceVoucher" parameterType="com.aidotnet.erp.fms.infrastructure.data.ExternalFinanceVoucherDO">
        INSERT INTO fms_external_finance_voucher (
            voucher_id, tenant_id, erp_voucher_id, finance_system, voucher_type, voucher_number,
            erp_reference_type, erp_reference_id, voucher_data_json, sync_status,
            sync_error, synced_at, created_at, updated_at
        )
        VALUES (
            #{voucherId}, #{tenantId}, #{erpVoucherId}, #{financeSystem}, #{voucherType}, #{voucherNumber},
            #{erpReferenceType}, #{erpReferenceId}, CAST(#{voucherDataJson} AS JSONB), #{syncStatus},
            #{syncError}, #{syncedAt}, #{createdAt}, #{updatedAt}
        )
    </insert>

    <update id="updateExternalFinanceVoucher" parameterType="com.aidotnet.erp.fms.infrastructure.data.ExternalFinanceVoucherDO">
        UPDATE fms_external_finance_voucher
        SET erp_voucher_id = #{erpVoucherId},
            finance_system = #{financeSystem},
            voucher_type = #{voucherType},
            voucher_number = #{voucherNumber},
            erp_reference_type = #{erpReferenceType},
            erp_reference_id = #{erpReferenceId},
            voucher_data_json = CAST(#{voucherDataJson} AS JSONB),
            sync_status = #{syncStatus},
            sync_error = #{syncError},
            synced_at = #{syncedAt},
            updated_at = #{updatedAt}
        WHERE tenant_id = #{tenantId}
          AND voucher_id = #{voucherId}
    </update>

    <select id="selectExternalFinanceVoucher" resultType="com.aidotnet.erp.fms.infrastructure.data.ExternalFinanceVoucherDO">
        SELECT voucher_id, tenant_id, erp_voucher_id, finance_system, voucher_type, voucher_number,
               erp_reference_type, erp_reference_id, CAST(voucher_data_json AS TEXT) AS voucher_data_json,
               sync_status, sync_error, synced_at, created_at, updated_at
        FROM fms_external_finance_voucher
        WHERE tenant_id = #{tenantId}
          AND voucher_id = #{voucherId}
    </select>

    <select id="selectExternalFinanceVouchers" resultType="com.aidotnet.erp.fms.infrastructure.data.ExternalFinanceVoucherDO">
        SELECT voucher_id, tenant_id, erp_voucher_id, finance_system, voucher_type, voucher_number,
               erp_reference_type, erp_reference_id, CAST(voucher_data_json AS TEXT) AS voucher_data_json,
               sync_status, sync_error, synced_at, created_at, updated_at
        FROM fms_external_finance_voucher
        WHERE tenant_id = #{tenantId}
        <if test="syncStatus != null and syncStatus != ''">
          AND sync_status = #{syncStatus}
        </if>
        ORDER BY created_at DESC, voucher_id DESC
    </select>

    <insert id="insertFinanceSyncConfig" parameterType="com.aidotnet.erp.fms.infrastructure.data.FinanceSyncConfigDO">
        INSERT INTO fms_finance_sync_config (
            config_id, tenant_id, finance_system, api_url, api_key, api_secret,
            account_set, enabled, mapping_rules_json, last_sync_at, created_at, updated_at
        )
        VALUES (
            #{configId}, #{tenantId}, #{financeSystem}, #{apiUrl}, #{apiKey}, #{apiSecret},
            #{accountSet}, #{enabled}, CAST(#{mappingRulesJson} AS JSONB), #{lastSyncAt}, #{createdAt}, #{updatedAt}
        )
    </insert>

    <update id="updateFinanceSyncConfig" parameterType="com.aidotnet.erp.fms.infrastructure.data.FinanceSyncConfigDO">
        UPDATE fms_finance_sync_config
        SET finance_system = #{financeSystem},
            api_url = #{apiUrl},
            api_key = #{apiKey},
            api_secret = #{apiSecret},
            account_set = #{accountSet},
            enabled = #{enabled},
            mapping_rules_json = CAST(#{mappingRulesJson} AS JSONB),
            last_sync_at = #{lastSyncAt},
            updated_at = #{updatedAt}
        WHERE tenant_id = #{tenantId}
          AND config_id = #{configId}
    </update>

    <select id="selectFinanceSyncConfig" resultType="com.aidotnet.erp.fms.infrastructure.data.FinanceSyncConfigDO">
        SELECT config_id, tenant_id, finance_system, api_url, api_key, api_secret,
               account_set, enabled, CAST(mapping_rules_json AS TEXT) AS mapping_rules_json,
               last_sync_at, created_at, updated_at
        FROM fms_finance_sync_config
        WHERE tenant_id = #{tenantId}
          AND config_id = #{configId}
    </select>

    <select id="selectFinanceSyncConfigBySystem" resultType="com.aidotnet.erp.fms.infrastructure.data.FinanceSyncConfigDO">
        SELECT config_id, tenant_id, finance_system, api_url, api_key, api_secret,
               account_set, enabled, CAST(mapping_rules_json AS TEXT) AS mapping_rules_json,
               last_sync_at, created_at, updated_at
        FROM fms_finance_sync_config
        WHERE tenant_id = #{tenantId}
          AND finance_system = #{financeSystem}
        LIMIT 1
    </select>

    <select id="selectFinanceSyncConfigs" resultType="com.aidotnet.erp.fms.infrastructure.data.FinanceSyncConfigDO">
        SELECT config_id, tenant_id, finance_system, api_url, api_key, api_secret,
               account_set, enabled, CAST(mapping_rules_json AS TEXT) AS mapping_rules_json,
               last_sync_at, created_at, updated_at
        FROM fms_finance_sync_config
        WHERE tenant_id = #{tenantId}
        ORDER BY updated_at DESC, config_id DESC
    </select>

    <insert id="insertCostAggregationRule" parameterType="com.aidotnet.erp.fms.infrastructure.data.CostAggregationRuleDO">
        INSERT INTO fms_cost_aggregation_rule (
            rule_id, tenant_id, rule_name, cost_source, cost_category,
            allocation_method, allocation_basis, target_dimension, enabled,
            priority, created_at, updated_at
        )
        VALUES (
            #{ruleId}, #{tenantId}, #{ruleName}, #{costSource}, #{costCategory},
            #{allocationMethod}, #{allocationBasis}, #{targetDimension}, #{enabled},
            #{priority}, #{createdAt}, #{updatedAt}
        )
    </insert>

    <update id="updateCostAggregationRule" parameterType="com.aidotnet.erp.fms.infrastructure.data.CostAggregationRuleDO">
        UPDATE fms_cost_aggregation_rule
        SET rule_name = #{ruleName},
            cost_source = #{costSource},
            cost_category = #{costCategory},
            allocation_method = #{allocationMethod},
            allocation_basis = #{allocationBasis},
            target_dimension = #{targetDimension},
            enabled = #{enabled},
            priority = #{priority},
            updated_at = #{updatedAt}
        WHERE tenant_id = #{tenantId}
          AND rule_id = #{ruleId}
    </update>

    <select id="selectCostAggregationRule" resultType="com.aidotnet.erp.fms.infrastructure.data.CostAggregationRuleDO">
        SELECT rule_id, tenant_id, rule_name, cost_source, cost_category,
               allocation_method, allocation_basis, target_dimension, enabled,
               priority, created_at, updated_at
        FROM fms_cost_aggregation_rule
        WHERE tenant_id = #{tenantId}
          AND rule_id = #{ruleId}
    </select>

    <select id="selectCostAggregationRules" resultType="com.aidotnet.erp.fms.infrastructure.data.CostAggregationRuleDO">
        SELECT rule_id, tenant_id, rule_name, cost_source, cost_category,
               allocation_method, allocation_basis, target_dimension, enabled,
               priority, created_at, updated_at
        FROM fms_cost_aggregation_rule
        WHERE tenant_id = #{tenantId}
        <if test="costSource != null and costSource != ''">
          AND cost_source = #{costSource}
        </if>
        ORDER BY priority DESC, created_at DESC
    </select>

    <insert id="insertCostAllocationResult" parameterType="com.aidotnet.erp.fms.infrastructure.data.CostAllocationResultDO">
        INSERT INTO fms_cost_allocation_result (
            result_id, tenant_id, rule_id, cost_event_id, target_dimension,
            target_id, allocated_amount, currency, exchange_rate,
            amount_in_base_currency, dimensions_json, allocated_at
        )
        VALUES (
            #{resultId}, #{tenantId}, #{ruleId}, #{costEventId}, #{targetDimension},
            #{targetId}, #{allocatedAmount}, #{currency}, #{exchangeRate},
            #{amountInBaseCurrency}, #{dimensionsJson}, #{allocatedAt}
        )
    </insert>

    <select id="selectCostAllocationResults" resultType="com.aidotnet.erp.fms.infrastructure.data.CostAllocationResultDO">
        SELECT result_id, tenant_id, rule_id, cost_event_id, target_dimension,
               target_id, allocated_amount, currency, exchange_rate,
               amount_in_base_currency, dimensions_json, allocated_at
        FROM fms_cost_allocation_result
        WHERE tenant_id = #{tenantId}
          AND target_dimension = #{targetDimension}
          AND target_id = #{targetId}
        ORDER BY allocated_at ASC, result_id ASC
    </select>

    <select id="selectCostAllocationResultsBySku" resultType="com.aidotnet.erp.fms.infrastructure.data.CostAllocationResultDO">
        SELECT result_id, tenant_id, rule_id, cost_event_id, target_dimension,
               target_id, allocated_amount, currency, exchange_rate,
               amount_in_base_currency, dimensions_json, allocated_at
        FROM fms_cost_allocation_result
        WHERE tenant_id = #{tenantId}
          AND dimensions_json LIKE CONCAT('%"sellerSku":"', #{sellerSku}, '"%')
        ORDER BY allocated_at DESC, result_id DESC
    </select>

    <select id="selectCostAllocationResultsByTarget" resultType="com.aidotnet.erp.fms.infrastructure.data.CostAllocationResultDO">
        SELECT result_id, tenant_id, rule_id, cost_event_id, target_dimension,
               target_id, allocated_amount, currency, exchange_rate,
               amount_in_base_currency, dimensions_json, allocated_at
        FROM fms_cost_allocation_result
        WHERE tenant_id = #{tenantId}
          AND target_dimension = #{dimensionType}
          AND target_id = #{dimensionId}
        ORDER BY allocated_at ASC, result_id ASC
    </select>

    <insert id="insertCostAnomaly" parameterType="com.aidotnet.erp.fms.infrastructure.data.CostAnomalyDO">
        INSERT INTO fms_cost_anomaly (
            anomaly_id, tenant_id, erp_reference_id, idempotency_key, anomaly_type,
            source_type, source_id, dimension_type, dimension_id, seller_sku,
            store_id, channel_code, marketplace_id, cost_type, suggested_amount,
            currency, auto_aggregate, reason, evidence_json, status,
            submitted_by, submitted_actor_type, approved_by, approved_at, effective_cost_event_id,
            allocation_result_ids_json, trace_id, purpose, raw_scope, created_at, updated_at
        )
        VALUES (
            #{anomalyId}, #{tenantId}, #{erpReferenceId}, #{idempotencyKey}, #{anomalyType},
            #{sourceType}, #{sourceId}, #{dimensionType}, #{dimensionId}, #{sellerSku},
            #{storeId}, #{channelCode}, #{marketplaceId}, #{costType}, #{suggestedAmount},
            #{currency}, #{autoAggregate}, #{reason}, #{evidenceJson}, #{status},
            #{submittedBy}, #{submittedActorType}, #{approvedBy}, #{approvedAt}, #{effectiveCostEventId},
            #{allocationResultIdsJson}, #{traceId}, #{purpose}, #{rawScope}, #{createdAt}, #{updatedAt}
        )
    </insert>

    <update id="updateCostAnomaly" parameterType="com.aidotnet.erp.fms.infrastructure.data.CostAnomalyDO">
        UPDATE fms_cost_anomaly
        SET erp_reference_id = #{erpReferenceId},
            idempotency_key = #{idempotencyKey},
            anomaly_type = #{anomalyType},
            source_type = #{sourceType},
            source_id = #{sourceId},
            dimension_type = #{dimensionType},
            dimension_id = #{dimensionId},
            seller_sku = #{sellerSku},
            store_id = #{storeId},
            channel_code = #{channelCode},
            marketplace_id = #{marketplaceId},
            cost_type = #{costType},
            suggested_amount = #{suggestedAmount},
            currency = #{currency},
            auto_aggregate = #{autoAggregate},
            reason = #{reason},
            evidence_json = #{evidenceJson},
            status = #{status},
            submitted_by = #{submittedBy},
            submitted_actor_type = #{submittedActorType},
            approved_by = #{approvedBy},
            approved_at = #{approvedAt},
            effective_cost_event_id = #{effectiveCostEventId},
            allocation_result_ids_json = #{allocationResultIdsJson},
            trace_id = #{traceId},
            purpose = #{purpose},
            raw_scope = #{rawScope},
            updated_at = #{updatedAt}
        WHERE tenant_id = #{tenantId}
          AND anomaly_id = #{anomalyId}
    </update>

    <select id="selectCostAnomaly" resultType="com.aidotnet.erp.fms.infrastructure.data.CostAnomalyDO">
        SELECT anomaly_id, tenant_id, erp_reference_id, idempotency_key, anomaly_type,
               source_type, source_id, dimension_type, dimension_id, seller_sku,
               store_id, channel_code, marketplace_id, cost_type, suggested_amount,
               currency, auto_aggregate, reason, evidence_json, status,
               submitted_by, submitted_actor_type, approved_by, approved_at, effective_cost_event_id,
               allocation_result_ids_json, trace_id, purpose, raw_scope, created_at, updated_at
        FROM fms_cost_anomaly
        WHERE tenant_id = #{tenantId}
          AND anomaly_id = #{anomalyId}
    </select>

    <select id="selectCostAnomalyByIdempotencyKey" resultType="com.aidotnet.erp.fms.infrastructure.data.CostAnomalyDO">
        SELECT anomaly_id, tenant_id, erp_reference_id, idempotency_key, anomaly_type,
               source_type, source_id, dimension_type, dimension_id, seller_sku,
               store_id, channel_code, marketplace_id, cost_type, suggested_amount,
               currency, auto_aggregate, reason, evidence_json, status,
               submitted_by, submitted_actor_type, approved_by, approved_at, effective_cost_event_id,
               allocation_result_ids_json, trace_id, purpose, raw_scope, created_at, updated_at
        FROM fms_cost_anomaly
        WHERE tenant_id = #{tenantId}
          AND idempotency_key = #{idempotencyKey}
        LIMIT 1
    </select>

    <select id="selectCostAnomalies" resultType="com.aidotnet.erp.fms.infrastructure.data.CostAnomalyDO">
        SELECT anomaly_id, tenant_id, erp_reference_id, idempotency_key, anomaly_type,
               source_type, source_id, dimension_type, dimension_id, seller_sku,
               store_id, channel_code, marketplace_id, cost_type, suggested_amount,
               currency, auto_aggregate, reason, evidence_json, status,
               submitted_by, submitted_actor_type, approved_by, approved_at, effective_cost_event_id,
               allocation_result_ids_json, trace_id, purpose, raw_scope, created_at, updated_at
        FROM fms_cost_anomaly
        WHERE tenant_id = #{tenantId}
        <if test="status != null and status != ''">
          AND status = #{status}
        </if>
        <if test="sellerSku != null and sellerSku != ''">
          AND seller_sku = #{sellerSku}
        </if>
        <if test="storeId != null and storeId != ''">
          AND store_id = #{storeId}
        </if>
        ORDER BY created_at DESC, anomaly_id DESC
    </select>

    <insert id="insertProfitResult" parameterType="com.aidotnet.erp.fms.infrastructure.data.ProfitResultDO">
        INSERT INTO fms_profit_result (
            result_id, tenant_id, dimension_type, dimension_id, seller_sku,
            order_id, store_id, marketplace_id, revenue, product_cost,
            shipping_cost, fba_fee, commission, advertising_cost, return_cost,
            storage_fee, packaging_cost, custom_duty, other_cost, total_cost,
            gross_profit, gross_margin, currency, exchange_rate,
            amount_in_base_currency, cost_details_json, calculated_at
        )
        VALUES (
            #{resultId}, #{tenantId}, #{dimensionType}, #{dimensionId}, #{sellerSku},
            #{orderId}, #{storeId}, #{marketplaceId}, #{revenue}, #{productCost},
            #{shippingCost}, #{fbaFee}, #{commission}, #{advertisingCost}, #{returnCost},
            #{storageFee}, #{packagingCost}, #{customDuty}, #{otherCost}, #{totalCost},
            #{grossProfit}, #{grossMargin}, #{currency}, #{exchangeRate},
            #{amountInBaseCurrency}, #{costDetailsJson}, #{calculatedAt}
        )
    </insert>

    <select id="selectProfitResult" resultType="com.aidotnet.erp.fms.infrastructure.data.ProfitResultDO">
        SELECT result_id, tenant_id, dimension_type, dimension_id, seller_sku,
               order_id, store_id, marketplace_id, revenue, product_cost,
               shipping_cost, fba_fee, commission, advertising_cost, return_cost,
               storage_fee, packaging_cost, custom_duty, other_cost, total_cost,
               gross_profit, gross_margin, currency, exchange_rate,
               amount_in_base_currency, cost_details_json, calculated_at
        FROM fms_profit_result
        WHERE tenant_id = #{tenantId}
          AND result_id = #{resultId}
    </select>

    <select id="selectProfitResults" resultType="com.aidotnet.erp.fms.infrastructure.data.ProfitResultDO">
        SELECT result_id, tenant_id, dimension_type, dimension_id, seller_sku,
               order_id, store_id, marketplace_id, revenue, product_cost,
               shipping_cost, fba_fee, commission, advertising_cost, return_cost,
               storage_fee, packaging_cost, custom_duty, other_cost, total_cost,
               gross_profit, gross_margin, currency, exchange_rate,
               amount_in_base_currency, cost_details_json, calculated_at
        FROM fms_profit_result
        WHERE tenant_id = #{tenantId}
          AND dimension_type = #{dimensionType}
          AND dimension_id = #{dimensionId}
        ORDER BY calculated_at DESC, result_id DESC
    </select>

    <select id="selectProfitResultsBySku" resultType="com.aidotnet.erp.fms.infrastructure.data.ProfitResultDO">
        SELECT result_id, tenant_id, dimension_type, dimension_id, seller_sku,
               order_id, store_id, marketplace_id, revenue, product_cost,
               shipping_cost, fba_fee, commission, advertising_cost, return_cost,
               storage_fee, packaging_cost, custom_duty, other_cost, total_cost,
               gross_profit, gross_margin, currency, exchange_rate,
               amount_in_base_currency, cost_details_json, calculated_at
        FROM fms_profit_result
        WHERE tenant_id = #{tenantId}
          AND seller_sku = #{sellerSku}
        ORDER BY calculated_at DESC, result_id DESC
    </select>

    <select id="selectAllProfitResults" resultType="com.aidotnet.erp.fms.infrastructure.data.ProfitResultDO">
        SELECT result_id, tenant_id, dimension_type, dimension_id, seller_sku,
               order_id, store_id, marketplace_id, revenue, product_cost,
               shipping_cost, fba_fee, commission, advertising_cost, return_cost,
               storage_fee, packaging_cost, custom_duty, other_cost, total_cost,
               gross_profit, gross_margin, currency, exchange_rate,
               amount_in_base_currency, cost_details_json, calculated_at
        FROM fms_profit_result
        WHERE tenant_id = #{tenantId}
        ORDER BY calculated_at DESC, result_id DESC
    </select>

    <select id="selectProfitResultsByDimension" resultType="com.aidotnet.erp.fms.infrastructure.data.ProfitResultDO">
        SELECT result_id, tenant_id, dimension_type, dimension_id, seller_sku,
               order_id, store_id, marketplace_id, revenue, product_cost,
               shipping_cost, fba_fee, commission, advertising_cost, return_cost,
               storage_fee, packaging_cost, custom_duty, other_cost, total_cost,
               gross_profit, gross_margin, currency, exchange_rate,
               amount_in_base_currency, cost_details_json, calculated_at
        FROM fms_profit_result
        WHERE tenant_id = #{tenantId}
          AND dimension_type = #{dimensionType}
        ORDER BY calculated_at DESC, result_id DESC
    </select>

    <insert id="insertProfitDeviationAlert" parameterType="com.aidotnet.erp.fms.infrastructure.data.ProfitDeviationAlertDO">
        INSERT INTO fms_profit_deviation_alert (
            alert_id, tenant_id, dimension_type, dimension_id, seller_sku,
            expected_margin, actual_margin, deviation, deviation_threshold,
            severity, status, detected_at, resolved_at
        )
        VALUES (
            #{alertId}, #{tenantId}, #{dimensionType}, #{dimensionId}, #{sellerSku},
            #{expectedMargin}, #{actualMargin}, #{deviation}, #{deviationThreshold},
            #{severity}, #{status}, #{detectedAt}, #{resolvedAt}
        )
    </insert>

    <update id="updateProfitDeviationAlert" parameterType="com.aidotnet.erp.fms.infrastructure.data.ProfitDeviationAlertDO">
        UPDATE fms_profit_deviation_alert
        SET expected_margin = #{expectedMargin},
            actual_margin = #{actualMargin},
            deviation = #{deviation},
            deviation_threshold = #{deviationThreshold},
            severity = #{severity},
            status = #{status},
            detected_at = #{detectedAt},
            resolved_at = #{resolvedAt}
        WHERE tenant_id = #{tenantId}
          AND alert_id = #{alertId}
    </update>

    <select id="selectProfitDeviationAlert" resultType="com.aidotnet.erp.fms.infrastructure.data.ProfitDeviationAlertDO">
        SELECT alert_id, tenant_id, dimension_type, dimension_id, seller_sku,
               expected_margin, actual_margin, deviation, deviation_threshold,
               severity, status, detected_at, resolved_at
        FROM fms_profit_deviation_alert
        WHERE tenant_id = #{tenantId}
          AND alert_id = #{alertId}
    </select>

    <select id="selectProfitDeviationAlerts" resultType="com.aidotnet.erp.fms.infrastructure.data.ProfitDeviationAlertDO">
        SELECT alert_id, tenant_id, dimension_type, dimension_id, seller_sku,
               expected_margin, actual_margin, deviation, deviation_threshold,
               severity, status, detected_at, resolved_at
        FROM fms_profit_deviation_alert
        WHERE tenant_id = #{tenantId}
        <if test="status != null and status != ''">
          AND status = #{status}
        </if>
        ORDER BY detected_at DESC, alert_id DESC
    </select>

    <insert id="insertCurrencyRateSyncLog" parameterType="com.aidotnet.erp.fms.infrastructure.data.CurrencyRateSyncLogDO">
        INSERT INTO fms_currency_rate_sync_log (
            sync_id, tenant_id, source, status, total_rates,
            success_count, fail_count, error_message, started_at, completed_at
        )
        VALUES (
            #{syncId}, #{tenantId}, #{source}, #{status}, #{totalRates},
            #{successCount}, #{failCount}, #{errorMessage}, #{startedAt}, #{completedAt}
        )
    </insert>

    <select id="selectCurrencyRateSyncLogs" resultType="com.aidotnet.erp.fms.infrastructure.data.CurrencyRateSyncLogDO">
        SELECT sync_id, tenant_id, source, status, total_rates,
               success_count, fail_count, error_message, started_at, completed_at
        FROM fms_currency_rate_sync_log
        WHERE tenant_id = #{tenantId}
        ORDER BY started_at DESC, sync_id DESC
    </select>

</mapper>
