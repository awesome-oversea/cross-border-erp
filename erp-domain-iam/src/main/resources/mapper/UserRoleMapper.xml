<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aidotnet.erp.iam.infrastructure.mapper.UserRoleMapper">

    <insert id="insert" parameterType="com.aidotnet.erp.iam.infrastructure.data.UserRoleDO">
        INSERT INTO iam_user_role (user_id, role_id, org_id, created_at)
        VALUES (#{userId}, #{roleId}, #{orgId}, #{createdAt})
    </insert>

    <delete id="delete">
        DELETE FROM iam_user_role
        WHERE user_id = #{userId} AND role_id = #{roleId}
    </delete>

    <select id="selectByUserId" resultType="com.aidotnet.erp.iam.infrastructure.data.UserRoleDO">
        SELECT user_id, role_id, org_id, created_at
        FROM iam_user_role
        WHERE user_id = #{userId}
    </select>

    <select id="selectByRoleId" resultType="com.aidotnet.erp.iam.infrastructure.data.UserRoleDO">
        SELECT user_id, role_id, org_id, created_at
        FROM iam_user_role
        WHERE role_id = #{roleId}
    </select>

</mapper>
