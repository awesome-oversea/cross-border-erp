<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aidotnet.erp.iam.infrastructure.mapper.TenantMapper">

    <insert id="insert" parameterType="com.aidotnet.erp.iam.infrastructure.data.TenantDO">
        INSERT INTO iam_tenant (tenant_id, name, status, created_at, updated_at)
        VALUES (#{tenantId}, #{name}, #{status}, #{createdAt}, #{updatedAt})
    </insert>

    <select id="selectById" resultType="com.aidotnet.erp.iam.infrastructure.data.TenantDO">
        SELECT tenant_id, name, status, created_at, updated_at
        FROM iam_tenant
        WHERE tenant_id = #{tenantId}
    </select>

    <select id="selectAll" resultType="com.aidotnet.erp.iam.infrastructure.data.TenantDO">
        SELECT tenant_id, name, status, created_at, updated_at
        FROM iam_tenant
        ORDER BY created_at
    </select>

    <update id="update" parameterType="com.aidotnet.erp.iam.infrastructure.data.TenantDO">
        UPDATE iam_tenant
        SET name = #{name}, status = #{status}, updated_at = #{updatedAt}
        WHERE tenant_id = #{tenantId}
    </update>

</mapper>
